apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget', '-q', '-O-', '-T', '3']
      args: ['http://{{ include "prometheus.fullname" . }}:{{ .Values.service.port }}/-/healthy']
  restartPolicy: Never