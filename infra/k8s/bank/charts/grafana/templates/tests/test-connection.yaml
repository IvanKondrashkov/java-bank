apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana.fullname" . }}-test-connection"
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget', '-q', '-O-', '-T', '3']
      args: ['http://{{ include "grafana.fullname" . }}:{{ .Values.service.port }}/api/health']
  restartPolicy: Never