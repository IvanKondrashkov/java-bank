apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gateway-service.fullname" . }}-test-connection"
  labels:
    {{- include "gateway-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl:8.6.0
      args:
        - "-fsS"
        - "http://{{ include "gateway-service.fullname" . }}:{{ .Values.service.port }}/actuator/health"
  restartPolicy: Never