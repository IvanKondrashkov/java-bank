apiVersion: v1
kind: Secret
metadata:
  name: {{ include "front-service.fullname" . }}-secret
  labels:
    {{- include "front-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  OAUTH_CLIENT_SECRET: {{ .Values.config.oauth.clientSecret | quote }}