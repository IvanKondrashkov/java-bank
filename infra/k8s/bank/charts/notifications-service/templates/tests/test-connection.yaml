apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notifications-service.fullname" . }}-test-connection"
  labels:
    {{- include "notifications-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl:8.6.0
      args:
        - "-fsS"
        - "http://{{ include "notifications-service.fullname" . }}:{{ .Values.service.port }}/actuator/health"
  restartPolicy: Never